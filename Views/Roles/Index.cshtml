@model IEnumerable<Microsoft.AspNetCore.Identity.IdentityRole>

<h2>@Localizer["RoleList"]</h2>
<table class="table">
    @foreach (var role in Model)
    {
        <tr>
            <td>@role.Name</td>
            <td>
                <form asp-action="Delete" asp-route-id="@role.Id" method="post">
                    <button type="submit" class="btn btn-sm btn-danger">
                        @Localizer["Delete"]
                    </button>
                </form>
            </td>
        </tr>
    }
</table>
<a class="btn btn-sm btn btn-primary mt-2" asp-action="Create">@Localizer["AddRole"]</a>
<a class="btn btn-sm btn btn-primary mt-2" asp-action="UserList">@Localizer["Users"]</a>